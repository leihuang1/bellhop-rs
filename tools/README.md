# Development tools

Reference download/build and differential-test scripts will be added with the numerical milestones. Ordinary parser tests use the curated fixtures committed under `crates/bellhop/tests/fixtures`.
