# Examples

- `field-g.json`: small self-contained coherent pressure-field case, suitable
  for CLI commands and HTTP examples.

It was canonicalized with `bellhop export` and requires no auxiliary files.
